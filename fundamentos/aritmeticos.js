//Um operador aritmeticos é binario.


//um destructuring
const [a, b, c ,d] = [3, 5, 1, 15]

//Sintaxe chamada como infix 1 + 1 + outro
const soma = a + b + c + d

const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)